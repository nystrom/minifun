# Interpreter for a dynamically typed functional language

To compile:

        stack build

The first time you build may take a long time as stack has to download other libraries.

To run:

        stack exec jsexp
        
